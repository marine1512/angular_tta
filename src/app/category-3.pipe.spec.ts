import { Category3Pipe } from './category-3.pipe';

describe('Category3Pipe', () => {
  it('create an instance', () => {
    const pipe = new Category3Pipe();
    expect(pipe).toBeTruthy();
  });
});
