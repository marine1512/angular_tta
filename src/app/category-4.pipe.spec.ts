import { Category4Pipe } from './category-4.pipe';

describe('Category4Pipe', () => {
  it('create an instance', () => {
    const pipe = new Category4Pipe();
    expect(pipe).toBeTruthy();
  });
});
