import { Category2Pipe } from './category-2.pipe';

describe('Category2Pipe', () => {
  it('create an instance', () => {
    const pipe = new Category2Pipe();
    expect(pipe).toBeTruthy();
  });
});
